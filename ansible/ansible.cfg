[defaults]
inventory          = inventories/proxmox/hosts.yml
roles_path         = roles
host_key_checking  = False

# Vault パスワードファイル（secrets/ に配置し、.gitignore で除外済み）
vault_password_file = secrets/vault_pass

[privilege_escalation]
become      = False
become_method = sudo
